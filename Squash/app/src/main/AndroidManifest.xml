<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asmaa.squash">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Teacher" />
        <activity android:name=".Professors" />
        <activity android:name=".professor_content" />
        <activity android:name=".ContentList" />
        <activity android:name=".ReadMe" />
        <activity android:name=".gallary_grid" />
        <activity android:name=".FullImage" />
        <activity android:name=".VideoLibrary" />
        <activity android:name=".FullVideo"></activity>
    </application>

</manifest>